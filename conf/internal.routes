# internal routes

# TODO - are we creating another routes file or putting this route into existing one
#POST        /call-back-from-upscan        controllers.internal.UpScanCallBackController.callBackFromUpscan()
